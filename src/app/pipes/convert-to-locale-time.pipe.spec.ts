import { ConvertToLocaleTimePipe } from './convert-to-locale-time.pipe';

describe('ConvertToLocaleTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToLocaleTimePipe();
    expect(pipe).toBeTruthy();
  });
});
